[build]
dockerfile = "Dockerfile"

# This section now only applies to the 'web' service
[services.web]
healthcheckPath = "/health"
restartPolicyType = "on_failure"

# This section applies to your worker
[services.worker]
healthcheckPath = ""  # Disables healthcheck for the worker
restartPolicyType = "on_failure"
